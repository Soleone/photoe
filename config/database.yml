development:
  adapter: mysql
  encoding: utf8
  database: myphotos_development
  username: root
  password:
  host: localhost
  socket: /tmp/mysql.sock

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: mysql
  encoding: utf8
  database: myphotos_test
  username: root
  password:
  host: localhost
  socket: /tmp/mysql.sock

production:
  adapter: mysql
  encoding: utf8
  database: myphotos_production
  username: root
  password:
  host: localhost
  socket: /tmp/mysql.sock
